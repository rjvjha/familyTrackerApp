# familyTrackerApp
A live location emitter app for family members using TLDB client.
Sending live location updates of the user to safe & chosen telegram user.
The app runs and performs location tracking even when killed and send a resuming location tracking notification when device get's restarted.
